# Password for WebREPL (FlowPy's WiFi deploy/patch path), read by
# webrepl.start() in boot.py. FlowPy authenticates with this automatically
# on every page load (default 'flowpy', in js/device.js) -- change both
# together if you want something other than the default. If they ever go
# out of sync, the browser will prompt for the right one on first use.
PASS = 'flowpy'
