# Edit these before uploading, then don't commit real credentials if this
# repo is or becomes public.
#
# Leave WIFI_SSID empty to skip joining a network entirely -- the board
# will start its own access point instead (see AP_SSID/AP_PASSWORD below).
WIFI_SSID = ''
WIFI_PASSWORD = ''

# Used only when WIFI_SSID is empty, or the join above fails.
AP_SSID = 'FlowPy'
AP_PASSWORD = 'flowpy123'
